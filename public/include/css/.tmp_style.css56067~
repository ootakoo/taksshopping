<html><h1>The H1 tag looks like this</h1>
<p>The paragraph tag after an H1 tag looks like this</p>
<h2>The H2 tag looks like this</h2>
<p>The paragraph tag after an H2 tag looks like this</p>
<h3>The H3 tag looks like this</h3>
<p>The paragraph tag after an H3 tag looks like this</p>
<h4>The H4 tag looks like this</h4>
<p>The paragraph tag after an H4 tag looks like this</p>
<h5>The H5 tag looks like this</h5>
<p>The paragraph tag after an H5 tag looks like this</p>
<ol>
<li><strong>This is a strong element in an ordered list</strong></li>
<li><i>This is an italic element in an ordered list</i></li>
<li><b>This is a bold element in an ordered list</b></li>
<li><em>This is an emphasized element in an ordered list</em></li>
<li>This is a regular element in an ordered list</li>
</ol>
<ul>
<li><strong>This is a strong element in an unordered list</strong></li>
<li><i>This is an italic element in an unordered list</i></li>
<li><b>This is a bold element in an unordered list</b></li>
<li><em>This is an emphasized element in an unordered list</em></li>
<li>This is a regular element in an unordered list</li>
</ul>
<blockquote>The block quote element looks like this</blockquote>
<pre>The pre element looks like this</pre>
<code>The code element looks like this</code>
<br/>
<span>The span element looks like this</span>
<br/>
<a href="#">The anchor element looks like this</a>
<br/>
<br/>
<div style="float:left;margin:20px;width:200px;">Text inside a div that is 200 pixels wide looks like this.  Text inside a div that is 200 pixels wide looks like this.  Text inside a div that is 200 pixels wide looks like this.  Text inside a div that is 200 pixels wide looks like this.  Text inside a div that is 200 pixels wide looks like this.  Text inside a div that is 200 pixels wide looks like this.  Text inside a div that is 200 pixels wide looks like this.  Text inside a div that is 200 pixels wide looks like this.  Text inside a div that is 200 pixels wide looks like this.  Text inside a div that is 200 pixels wide looks like this.</div>
<div style="float:left;margin:20px;width:400px;">Text inside a div that is 400 pixels wide looks like this.  Text inside a div that is 400 pixels wide looks like this.  Text inside a div that is 400 pixels wide looks like this.  Text inside a div that is 400 pixels wide looks like this.  Text inside a div that is 400 pixels wide looks like this.  Text inside a div that is 400 pixels wide looks like this.  Text inside a div that is 400 pixels wide looks like this.  Text inside a div that is 400 pixels wide looks like this.  Text inside a div that is 400 pixels wide looks like this.  Text inside a div that is 400 pixels wide looks like this.  Text inside a div that is 400 pixels wide looks like this.  Text inside a div that is 400 pixels wide looks like this.  Text inside a div that is 400 pixels wide looks like this.  Text inside a div that is 400 pixels wide looks like this.  Text inside a div that is 400 pixels wide looks like this.  Text inside a div that is 400 pixels wide looks like this.  Text inside a div that is 400 pixels wide looks like this.  Text inside a div that is 400 pixels wide looks like this.  Text inside a div that is 400 pixels wide looks like this.  Text inside a div that is 400 pixels wide looks like this.  Text inside a div that is 400 pixels wide looks like this.  Text inside a div that is 400 pixels wide looks like this.</div>
<div style="float:left;margin:20px;width:600px;">Text inside a div that is 600 pixels wide looks like this.  Text inside a div that is 600 pixels wide looks like this.  Text inside a div that is 600 pixels wide looks like this.  Text inside a div that is 600 pixels wide looks like this.  Text inside a div that is 600 pixels wide looks like this.  Text inside a div that is 600 pixels wide looks like this.  Text inside a div that is 600 pixels wide looks like this.  Text inside a div that is 600 pixels wide looks like this.  Text inside a div that is 600 pixels wide looks like this.  Text inside a div that is 600 pixels wide looks like this.  Text inside a div that is 600 pixels wide looks like this.  Text inside a div that is 600 pixels wide looks like this.  Text inside a div that is 600 pixels wide looks like this.  Text inside a div that is 600 pixels wide looks like this.  Text inside a div that is 600 pixels wide looks like this.  Text inside a div that is 600 pixels wide looks like this.  Text inside a div that is 600 pixels wide looks like this.  Text inside a div that is 600 pixels wide looks like this.  Text inside a div that is 600 pixels wide looks like this.  Text inside a div that is 600 pixels wide looks like this.  Text inside a div that is 600 pixels wide looks like this.  Text inside a div that is 600 pixels wide looks like this.  Text inside a div that is 600 pixels wide looks like this.  Text inside a div that is 600 pixels wide looks like this.  Text inside a div that is 600 pixels wide looks like this.  Text inside a div that is 600 pixels wide looks like this.  Text inside a div that is 600 pixels wide looks like this.  Text inside a div that is 600 pixels wide looks like this.  Text inside a div that is 600 pixels wide looks like this.  Text inside a div that is 600 pixels wide looks like this.  Text inside a div that is 600 pixels wide looks like this.  Text inside a div that is 600 pixels wide looks like this.  Text inside a div that is 600 pixels wide looks like this.</div><style>@charset "utf-8";
@import "normalize.css";
/*-----------------------------------------*/
html, body {
	height:100%;
	background-color:white;
}

body {
    position: absolute;
    left: 50%;
    width: 1024px;
    margin-left: -512px;
	height:100%;
}

body ul, body ol {
    list-style: none;
	padding:0px;
	margin:0px;
}

a:LINK, a:VISITED{
	text-decoration: none;
	color:#4A4A4A;
	}


/* 아이템 네비부분 */
 header {
	height:100%;
    position: fixed;
    overflow-y: visual;
    width: 200px;
    
	
    
    background: #F0F0F0;
    border: solid #CACACA 1px;
    
   
}


 header ol li{
 	
 	height:49px;
	font: 15px / 3 NanumGothicWeb ;
	margin:5px;
	
	border-radius:3px;
	border-top:white solid 1px;
	border-left:#D4D4D4 solid 1px;
	border-right:#D4D4D4 solid 1px;
	border-bottom:#D4D4D4 solid 1px;
	text-shadow:0px 1px 0px white;
	
background: rgb(255,255,255); /* Old browsers */
background: -moz-linear-gradient(top,  rgba(255,255,255,1) 0%, rgba(244,244,244,1) 100%); /* FF3.6+ */
background: -webkit-gradient(linear, left top, left bottom, color-stop(0%,rgba(255,255,255,1)), color-stop(100%,rgba(244,244,244,1))); /* Chrome,Safari4+ */
background: -webkit-linear-gradient(top,  rgba(255,255,255,1) 0%,rgba(244,244,244,1) 100%); /* Chrome10+,Safari5.1+ */
background: -o-linear-gradient(top,  rgba(255,255,255,1) 0%,rgba(244,244,244,1) 100%); /* Opera 11.10+ */
background: -ms-linear-gradient(top,  rgba(255,255,255,1) 0%,rgba(244,244,244,1) 100%); /* IE10+ */
background: linear-gradient(top,  rgba(255,255,255,1) 0%,rgba(244,244,244,1) 100%); /* W3C */
filter: progid:DXImageTransform.Microsoft.gradient( startColorstr='#ffffff', endColorstr='#f4f4f4',GradientType=0 ); /* IE6-9 */


}

header ol li a{
	padding:0px 0px 0px 15px;
	display:block;
	width:auto;
	height:51px;
	border:white;
}



header ol li:hover a,
header ol li:focus a,header .active a{
	color:#fff;
	
	text-shadow:none;
	


}

header ol li:hover,
header ol li:focus,header .active{
	background: #34A6D8;
	/*border:#f747e5 1px solid;
	box-shadow:0px 2px 10px #f747e5 inset; 
	 /* Old browsers */
/*background: -moz-linear-gradient(top,  rgba(255,101,242,1) 0%, rgba(255,101,242,1) 100%); /* FF3.6+ */
/*background: -webkit-gradient(linear, left top, left bottom, color-stop(0%,rgba(255,101,242,1)), color-stop(100%,rgba(255,101,242,1))); /* Chrome,Safari4+ */
/*background: -webkit-linear-gradient(top,  rgba(255,101,242,1) 0%,rgba(255,101,242,1) 100%); /* Chrome10+,Safari5.1+ */
/*background: -o-linear-gradient(top,  rgba(255,101,242,1) 0%,rgba(255,101,242,1) 100%); /* Opera 11.10+ */
/*background: -ms-linear-gradient(top,  rgba(255,101,242,1) 0%,rgba(255,101,242,1) 100%); /* IE10+ */
/*filter: progid:DXImageTransform.Microsoft.gradient( startColorstr='#ff65f2', endColorstr='#ff65f2',GradientType=0 ); /* IE6-9 */ 
}





 header #brand {
    text-indent: -10000px;
	height:210px;
    background: url(../images/logo.png) no-repeat;
    background-size: cover;
}


/* 컨텐츠부분*/


 #contents {
    
	position: absolute;
    left: 202px;
	min-height:100%;
}


/* 탭 메뉴 부분 */

 #contents #tab_wrap {
	position: fixed; 
    height: 110px;
    font: 15px /1.5 NanumGothicWeb ;
   	background-color:white;
    border-right:#CACACA solid 1px;
	border-top:#CACACA solid 1px;
     z-index:3;
} 



 #contents #tab_nav ul li{
 
 		
	        float: left;
			width:266px;
			height:50px;
			margin:0px;
			border-right:#CACACA solid 1px;
			 border-bottom:#CACACA solid 1px;
			 
			 
			
			background: rgb(255,255,255); /* Old browsers */
background: -moz-linear-gradient(top,  rgba(255,255,255,1) 0%, rgba(244,244,244,1) 100%); /* FF3.6+ */
background: -webkit-gradient(linear, left top, left bottom, color-stop(0%,rgba(255,255,255,1)), color-stop(100%,rgba(244,244,244,1))); /* Chrome,Safari4+ */
background: -webkit-linear-gradient(top,  rgba(255,255,255,1) 0%,rgba(244,244,244,1) 100%); /* Chrome10+,Safari5.1+ */
background: -o-linear-gradient(top,  rgba(255,255,255,1) 0%,rgba(244,244,244,1) 100%); /* Opera 11.10+ */
background: -ms-linear-gradient(top,  rgba(255,255,255,1) 0%,rgba(244,244,244,1) 100%); /* IE10+ */
background: linear-gradient(top,  rgba(255,255,255,1) 0%,rgba(244,244,244,1) 100%); /* W3C */
			
			 
}
#contents #tab_nav ul li a{
		display:block;
		width:266px;
		padding:10px 0px;
		text-align:center;
	}
 #contents #tab_nav ul li:last-child{
	 border-right:none;
	 width:268px;
}

 #contents #tab_nav ul li:hover ,
 #contents #tab_nav ul li:focus{
 	
	
 }


#contents #tab_wrap #tab_nav li.active{
background: -moz-linear-gradient(top,  rgba(255,255,255,1) 0%, rgba(255,255,255,0) 100%); /* FF3.6+ */
background: -webkit-gradient(linear, left top, left bottom, color-stop(0%,rgba(255,255,255,1)), color-stop(100%,rgba(255,255,255,0))); /* Chrome,Safari4+ */
background: -webkit-linear-gradient(top,  rgba(255,255,255,1) 0%,rgba(255,255,255,0) 100%); /* Chrome10+,Safari5.1+ */
background: -o-linear-gradient(top,  rgba(255,255,255,1) 0%,rgba(255,255,255,0) 100%); /* Opera 11.10+ */
background: -ms-linear-gradient(top,  rgba(255,255,255,1) 0%,rgba(255,255,255,0) 100%); /* IE10+ */
background: linear-gradient(top,  rgba(255,255,255,1) 0%,rgba(255,255,255,0) 100%); /* W3C */
filter: progid:DXImageTransform.Microsoft.gradient( startColorstr='#ffffff', endColorstr='#00ffffff',GradientType=0 ); /* IE6-9 */

    border-bottom:white solid 1px;
	
}

#tab_state{
	 float: left;
	 width:760px;
	 margin-left:15px;
	 max-height:50px;
	 border-bottom:#cacaca 1px solid;
	 border-style:
}
#tab_state_sub{
	float: left;
	width:260px;
	height:50px;
}
#helper{
	float: left;
	width:480px;
	height:30px;
	margin:8px 0px 0px 0px;
	padding:5px 0px 0px 10px ;
	color:white;
	
	border-radius:5px;
	
	background-color: #34A6D8;
}

/*아이템부분*/
#contents{
	
	width:803px;
	height:100%;
}

#contents .tab_contents{
	position:fixed;
	margin:110px 0px 0px 0px;
	border-right:#CACACA solid 1px;
	width:802px;
	height:100%;
	overflow-y:;
	
	
}
#contents .tab_contents li{
	position:absolute;
	width:802px;
	background-color:white;
}
#contents .tab_contents li.active{
}
#contents .tab_contents .item_list ul li {
	position:relative;
	float:left;
	width:180px;
	height:180px;
	border-radius:10px;
	border:1px solid #cacaca;
	box-shadow:0px 1px 2px #cacaca;
	margin:5px;
	
}
#contents .tab_contents .item_list ul li img{
	position:absolute;
	width:180px;
	height:180px;
	border-radius:10px;
	z-index: 1;
}
#contents  .item_list ul li .item{
	position:relative;
	top:160px;
	width:180px;
	height:20px;
	background-color:rgba(255,255,255,0.8);
	border-radius:0px 0px 10px 10px ;
	z-index: 2;
	color:#272727;
	text-align:center;
	font: 13px/1.5 NanumGothicWeb ;
	
	
}

/* ///////////////////////////////////////// 
	=.Global Classes
///////////////////////////////////////// */
.clearfix:after {
  content: "";
  display: block;
  clear: both;
}
.ie6 .clearfix {
  height: 1px;
}
.ie7 .clearfix {
  min-height: 1px;
}
.blind {
  visibility: hidden;
  position: absolute;
  top: -10000px;
  height: 1px;
  width: 1px;
}
</style></html>